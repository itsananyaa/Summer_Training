#include<iostream>
#include<vector>
using namespace std;

int main()
{
    int n;
    cout << "Enter Number of Elements to be inputed: ";
    cin >> n;
    vector<int> occurence(n);

    for(auto i = 0; i < occurence.size(); i++)
    {
        cin >> occurence[i];
    }
    for(auto i = 0; i < occurence.size() - 1; i++)
    {
        for(auto j = i + 1; j < occurence.size();)
        {
            if(occurence[i] == occurence[j])
            {
                occurence.erase(occurence.begin() + j);
            }
            else
            {
                j++;
            }
        }
    }
    for(auto i = 0; i < occurence.size(); i++)
    {
        cout << occurence[i] << "\t";
    }
    return 0;
}
